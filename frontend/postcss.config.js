// PostCSS procesa el CSS y permite usar plugins como Tailwind.
export default {
  plugins: {
    tailwindcss: {}, // Activa Tailwind CSS como plugin de PostCSS.
    autoprefixer: {}, // Agrega prefijos de navegador automáticamente al CSS.
  },
}
